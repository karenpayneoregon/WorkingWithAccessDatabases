This project shows how to use read related tables.

Points of interest
	* Using one connection with two command's, two readers.
	* Keeping related data in sync with concrete classes instead of the typical DataSet/BindingSource method to show related data.